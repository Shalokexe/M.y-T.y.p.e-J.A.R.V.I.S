# Copy this file to .env only when the Flask backend is added.
# Never commit real credentials.
FLASK_ENV=development
SECRET_KEY=replace-with-a-local-secret
OPENAI_API_KEY=
WEATHER_API_KEY=
PORT=5000
